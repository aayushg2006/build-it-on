import LiveQuotePhase from "./LiveQuotePhase";

const LiveRegistration = () => {
  return (
    <LiveQuotePhase
      phaseTitle="Registration Desk Open"
      headline="Registration"
      quote="Welcome builders. Check in, grab your badge, and get projector-ready."
    />
  );
};

export default LiveRegistration;
