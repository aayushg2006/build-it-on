import LiveQuotePhase from "./LiveQuotePhase";

const LivePrize = () => {
  return (
    <LiveQuotePhase
      phaseTitle="Prize Distribution & Closing"
      headline="Prize Distribution"
      quote="Celebrate the effort, the learning, and the teams that pushed the frontier."
    />
  );
};

export default LivePrize;
