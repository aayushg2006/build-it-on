import LiveQuotePhase from "./LiveQuotePhase";

const LiveLunch = () => {
  return (
    <LiveQuotePhase
      phaseTitle="Lunch Break"
      headline="Lunch"
      quote="Recharge, reset, and come back sharper for Round 2."
    />
  );
};

export default LiveLunch;
