import LiveQuotePhase from "./LiveQuotePhase";

const LiveInauguration = () => {
  return (
    <LiveQuotePhase
      phaseTitle="Inauguration Ceremony"
      headline="Inauguration"
      quote="Today we build ideas that can become tomorrow's impact."
    />
  );
};

export default LiveInauguration;
